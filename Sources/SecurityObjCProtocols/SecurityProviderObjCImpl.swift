import Foundation

/// Base protocol for ObjC-compatible security provider implementations
@objc public protocol SecurityProviderObjCImpl {
    // Empty protocol to allow for protocol composition
    // This helps break circular dependencies
}
